# Changesets

This directory holds [Changesets](https://github.com/changesets/changesets)
markdown files. Each user-visible change should ship with a changeset
describing what's changing and the SemVer bump it implies.

Run `pnpm changeset` to add one interactively. Pure-internal refactors don't
need a changeset; mark them `chore` if you do add one.
